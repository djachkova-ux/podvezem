# Firebase Functions читает functions/.env(.<projectId>) автоматически при
# деплое и в рантайме — копировать в functions/.env, в git не коммитить.

# Абсолютный адрес хостинга (появится в S12) — без него push отправляются
# без ссылки перехода на конкретный экран (см. lib/messaging.js).
APP_URL=https://podvezyom.web.app
