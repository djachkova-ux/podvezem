# Хэндофф · «Подвезём» (школьный трансфер)

Дата: 02.09.2026. Сессия: S0 — подготовка (ТЗ, дизайн, репозиторий).

## Что сделано

- **ТЗ** `ТЗ-Школьный-трансфер.md` — роли, очереди, две доски, логика откликов в обе стороны, завершение поездки, уведомления. Раздел 2.1 — утверждённое дизайн-решение.
- **Дизайн-макет** доски водителей `design/board-drivers.html` — мобильный экран целиком, светлая и тёмная тема, состояния карточки. Опубликован артефактом: https://claude.ai/code/artifact/5892fcf8-abe8-4777-9b36-57607a9552c0
- **Токены** `design/tokens.css` — цвета, шрифты, радиусы для кода. Литеральных цветов в вёрстке быть не должно, только переменные.
- **Трекер** `ТРЕКЕР-РАЗРАБОТКИ.md` — 12 сессий и правила экономии контекста.
- **Git** — репозиторий инициализирован, первый коммит сделан.

## Принятые решения

- Стек: React + Vite + Firebase (Firestore, Auth, Cloud Messaging, Hosting). Шаблон «интеграция кефир» здесь **не применяется** — это не лендинг, а многопользовательское приложение.
- Вход: телефон + пароль, **без** SMS-подтверждения (бесплатно, сообщество закрытое).
- Регистрация открытая, без модерации админом. В модели данных оставляем задел под роль admin и блокировку, интерфейса пока нет.
- Палитра «Хвоя» (хвойный + латунь) выбрана из трёх предложенных вариантов. Переключатель палитр из макета убран.
- Шрифты: Golos Text (интерфейс) + Oswald (время и служебные метки).

## Состояние репозитория

Приватный репозиторий: **https://github.com/djachkova-ux/podvezem**, ветка `main`, первый коммит запушен, локальная ветка синхронизирована с `origin/main`.

Особенности окружения, чтобы не искать заново:

- GitHub CLI установлен, но **не в PATH** — вызывать по полному пути: `& "C:\Program Files\GitHub CLI\gh.exe"`. Авторизован аккаунт `djachkova-ux` (scopes: repo, workflow, gist, read:org).
- Git-идентичность задана **только для этого репозитория** (глобальной на машине нет): `alati74` / `alati74@gmail.com`. Если на GitHub у аккаунта другой основной e-mail, коммиты не будут привязываться к профилю — тогда поменять `git config user.email`.

## Следующая сессия — S1 (каркас проекта и Firebase)

Начать так: `Читай ТРЕКЕР-РАЗРАБОТКИ.md, делаем сессию S1`.

**От пользователя понадобится:** проект в Firebase Console (Firestore + Authentication + Hosting включены) и ключи веб-приложения — они лягут в `.env.local`, который в `.gitignore`.

## На что обратить внимание дальше

- **Транзакции в S6.** Подтверждение отклика уменьшает свободные места — только через `runTransaction`, иначе два одновременных подтверждения уведут места в минус.
- **Push на iPhone** работает только для приложения, добавленного на «Домой» (iOS 16.4+); во вкладке Safari уведомления не приходят. Тестировать именно в установленном виде.
- **Макет не читать целиком** в следующих сессиях — это ≈40 000 токенов. Смотреть в браузере, цвета брать из `design/tokens.css`.
