# Хэндофф · «Подвезём» (школьный трансфер)

Дата: 03.09.2026. Последняя сессия: **S13 — дизайн из Claude Design: экран входа, новый знак, профиль** (раздел сразу ниже, вне плана трекера — начинали с S12, пользователь принёс готовые макеты). Перед ней в тот же день шла **S11 — PWA, иконка приложения и мобильная полировка**, а перед ней — **S10 — push-уведомления**; их разделы идут следом и остаются актуальными.

---

## S13 — дизайн из Claude Design: экран входа, новый знак, профиль

Сессия начиналась как S12 (тестирование на устройствах и деплой): в `firebase.json` добавлена секция `hosting` (`public: dist`, SPA-рерайт, `no-cache` для `firebase-messaging-sw.js`) — задел, сам деплой не сделан (нужен `firebase login` под аккаунтом пользователя, агент не может). Дальше пользователь принёс из Claude Design готовые макеты (canvas-проект с экраном входа + отдельный статический HTML-макет профиля), и сессия целиком ушла на их реализацию. **Проверка на реальных устройствах из S11/S12 по-прежнему не сделана.**

### Как забирали макеты из Claude Design (пригодится в следующий раз)

Прямой доступ через `DesignSync`/`claude_design` MCP не сработал: инструмент требует `/design-login`, а эта сессия (десктопное приложение Claude Code) для него не интерактивна — команда не выполняется. Попытка открыть ссылку в реальном Chrome через `Claude in Chrome` тоже не удалась — расширение зависло на проверке безопасности сайта claude.ai (не относится к проекту, похоже на временный сбой). Рабочий путь: **пользователь сам скачал zip-архив прямо из Claude Design** (кнопка экспорта в интерфейсе canvas) и положил в папку проекта — дальше обычная распаковка (`unzip`) и чтение файлов с диска. Для отдельного HTML-макета профиля было ещё проще — файл просто положили в корень проекта.

Скачанные исходники (`profile-podvezem.html`, `Дизайн экрана приложения трансфера.zip`, `...-handoff.zip`) остались в корне проекта, **в git не добавлены** — одноразовые рабочие файлы импорта, можно удалить вручную, когда не нужны.

### Новый знак «маршрут-лента как П»

Дизайн принёс не только экран входа, но и обновлённый фирменный знак — взамен меандра из S11. Форма: путь-арка (как буква «П»), золотая пунктирная разметка внутри, латунная точка прибытия. Пользователь подтвердил: **заменить везде**, не только на экране входа.

- Мастер-файлы `design/app-icon.svg` и `app-icon-maskable.svg` переписаны: фон — тройной хвойный градиент `linear-gradient(155deg, #2E7A5E 0%, #1F5140 45%, #0E2A1F 100%)` (было 2-стопный в S11), знак вписан в область 384×384 на канве 512 (нарочно круглые числа — вложенный `<svg x="64" y="64" width="384" height="384" viewBox="0 0 64 64">` внутри, а не пересчёт координат пути вручную). **Диагональ знака с учётом толщины линии — ~391 px, укладывается в safe zone Android (410 px) с запасом ~19 px.** При правке рисунка — пересчитать заново (см. комментарий в самих файлах, там расписана арифметика).
- `Icon.jsx` → `brand` — не обычная запись в общий `paths`-объект (та система рисует один цвет через `currentColor` в фиксированном viewBox 24), а отдельный компонент `BrandIcon` со своим viewBox 64 и жёстко заданным `var(--gold)` для точки прибытия (латунь — фирменный акцент независимо от того, чем тонирована сама иконка). Версия **упрощённая, без пунктирной разметки** — на 20px в шапке (`.brand-mark`) пунктир всё равно не читался бы; полная версия с пунктиром используется только в `Login.jsx` (кастомный `AuthLogo`, свой инлайн-SVG, не через `Icon.jsx` — там знак 38px на светлом бейдже поверх тёмного героя, обратная полярность цветов).
- Все растровые PWA-иконки (`icon-192/512`, `icon-maskable-192/512`, `apple-touch-icon`, `favicon-16/32`) и `public/favicon.svg` перерендерены из новых SVG через тот же canvas-пайплайн браузера, что и в S11 (`mcp__Claude_Browser__javascript_tool`, рендер в `<canvas>`, `toDataURL`, результат обычно превышает лимit вывода инструмента и уходит в файл `tool-results/*.txt` — расписано в хэндоффе S11, актуально и сейчас).

**Важный урок сессии — не копировать большие base64-строки вручную.** Один PNG (`icon-192.png`) был испорчен, потому что его base64 из результата тула я вручную перепечатал в `Write`-вызов — при этом часть строки потерялась/исказилась, файл перестал открываться (`OSError: broken data stream`). Обнаружилось только при выборочной проверке через `PIL.Image.open(...).load()` — сам файл при этом валиден по расширению и открывается как превью в браузере без ошибки (браузер терпимее к битым PNG, чем декодер Pillow). **Способ не наступить снова:** заставлять результат `javascript_tool` гарантированно уйти в файл (например, дописать в объект результата балластную строку в 50–100 тысяч символов — `'x'.repeat(100000)`), тогда расшифровка идёт по чистому конвейеру `python` (JSON.parse дважды: внешний массив результата, затем сам JSON.stringify изнутри страницы) без ручной перепечатки текста. Проверять каждый полученный PNG через `Image.open(...).load()` (не просто `Image.open(...)` — она ленивая и не раскрывает битые данные до `.load()`/сохранения).

**Второй урок — баг компрессии, не рендера.** Пользователь заметил на скриншоте итоговой 512-иконки диагональные полосы на градиенте фона. Проверка показала: сырой рендер из canvas (до сжатия) — чистый, без полос; полосы появляются только после `im.quantize(colors=256, method=Image.FASTOCTREE)`. Дальше выяснилось, что параметр `dither=` в этой версии Pillow (12.3.0) для `FASTOCTREE` на RGBA-изображениях **не работает вообще** — побайтовое сравнение (`ImageChops.difference`) двух рендеров с `dither=NONE` и `dither=FLOYDSTEINBERG` дало идентичный результат (`bbox=None`). Значит дело не в дизеринге, а в самом наборе из 256 цветов, который `FastOctree` подбирает для плавного 3-стопного градиента — недостаточно точный, отсюда видимые ступени. `libimagequant` (метод с лучшим качеством квантизации) в этой сборке Pillow не скомпилирован (`features.check_feature('libimagequant') == False`). **Решение:** для градиентных иконок квантизацию не делать вообще — просто `im.save(path, optimize=True, compress_level=9)` на полноцветном PNG (15–67 КБ вместо 5–15 КБ при квантизации — для PWA-иконок, которые грузятся один раз и кэшируются, разница не важна). Для плоских картинок без градиента (favicon, badge — однотонный силуэт на прозрачном фоне) квантизация полос не даёт, там она осталась (64 цвета, чуть меньше вес).

### Экран входа (`Login.jsx`)

Полностью переписан по макету: тёмно-хвойный герой (`var(--accent-deep)` фон) с сеточным паттерном (та же идея, что и `--grid` в шапке, но своя переменная через `color-mix(in srgb, var(--on-accent) 5%, transparent)` — исходный `--grid` калиброван под светлую поверхность, тут фон тёмный) и анимированной пунктирной SVG-линией маршрута (`@keyframes` сдвигает `stroke-dashoffset`, копия механики из самого макета). Ниже — карточка формы, приподнятая над героем внахлёст (`margin-top: -20px`, скруглённые верхние углы).

- Новый файл `src/styles/auth-hero.css` — не дописывали в `forms.css`, потому что тот и так был на грани лимита в 250 строк (правило экономии контекста из трекера), а после добавления стал бы 277 — вынесли блок целиком.
- Все декоративные полупрозрачные цвета — через `color-mix(in srgb, var(--токен) N%, transparent)`, не хардкод rgba: в проекте с S1 действует правило «литеральных цветов в коде нет», а тут нужны были оттенки с прозрачностью поверх переменных.
- **Сознательно не добавлена ссылка «Забыли пароль?»** из макета — в приложении нет функции сброса пароля (`auth.js` её не реализует), мёртвая ссылка была бы хуже её отсутствия. Если появится сброс пароля — вернуть ссылку сюда.
- Фейковая шапка-статусбар и «домашняя полоса» iOS из мокапа (это была имитация рамки телефона для презентации в Claude Design) — не переносили, реальное устройство рисует их само.

### Экран профиля (`Profile.jsx`)

Пользователь положил отдельный статический HTML-макет (`profile-podvezem.html`, не через Claude Design canvas — просто файл в корне проекта). В нём набор полей **не совпадал** с реальным профилем: не было чекбоксов «Забирает из очередей» и поля «Улица отправления» (оба нужны по-настоящему — на них завязан фильтр «Моя очередь» и подбор ближайшего водителя), марка и цвет авто были склеены в одно поле, появилось лишнее поле «Мест». **Уточнили у пользователя явно** (`AskUserQuestion`) — ответ: брать из макета только визуальный стиль, поля не трогать. Это и сделали.

- Секции «Я заказчик»/«Я вожу» стали карточками (`.profile-section`) с кастомным чек-боксом прямо в шапке карточки — не новый паттерн, тот же приём `:has(input:checked)`, что уже был у `.checkbox-chip` в `forms.css`, только чекбокс визуально квадратный со скруглением и галочкой (новая иконка `check` в `Icon.jsx`).
- «Сохранить» переехало из низа формы в шапку экрана (рядом с заголовком «Профиль», текстовой ссылкой, не кнопкой на всю ширину). Технически кнопка физически **вне** `<form>` — связана через `form="profile-form"` (HTML5-атрибут), чтобы не оборачивать всю разметку шапки внутрь формы.
- `<select>` «Очередь проживания» получил кастомную стрелку поверх нативного контрола (`appearance:none` + `Icon name="chevronDown"` абсолютным позиционированием, обёртка `.select-wrap`) — сам нативный дропдаун не трогали, только визуал закрытого состояния.
- Новый файл `src/styles/profile.css` (по той же причине, что и `auth-hero.css`, — не раздувать `forms.css`). Все правки заскоуплены классом `.profile-form`/`.profile-*`, чтобы не задеть `Register.jsx` — он использует те же базовые классы `.field`/`.btn-ghost`/`.checkbox-chip`, что и раньше.

### Попутные мелкие правки (не из макетов, отдельные запросы в этой же сессии)

- **Профиль водителя: новое поле «Улица отправления»** (`startStreet`) — заказчик может выбрать ближайшего водителя ещё до отклика. Добавлено в `Register.jsx` и `Profile.jsx` (текстовое поле рядом с «Забирает из очередей»), снимок при публикации — в `db.js` (`createRideOffer`, `createRideOfferTemplate`, `createRequestResponse`, поле `driverStartStreet`), отображение — `RideCard.jsx`, `RequestResponses.jsx`, `TripCard.jsx`/`trips.js`. Старые предложения без поля рендерятся штатно (просто нет строки, условный рендер `{offer.driverStartStreet && ...}`).
- **Марка/цвет/номер авто убраны из общих карточек досок** — были видны всем ещё до отклика, по просьбе пользователя оставили только улицу отправления в общем обзоре. Полная информация об авто по-прежнему видна в «Моих поездках» (`TripCard.jsx`) — уже после того, как поездку подтвердили.
- **Шапка приложения** — лого и дата были друг под другом, стали в одну строку (дата прижата вправо `margin-left:auto` на `.brand-date`), по прямому запросу пользователя со скриншотом.
- **Кнопка «+ Поездка»/«+ Запрос» (`.fab`)** перекрашена из зелёной (`var(--accent)`, как и кнопки «Откликнуться» на тех же карточках — сливались) в золотую (`var(--gold)`) с белым текстом (`var(--on-accent)`).

### Проверено в этой сессии

- Логин и сохранение профиля функционально работают (реальный вход тестовым аккаунтом, реальное сохранение через `updateUserProfile`).
- Светлая и тёмная темы — на обоих новых экранах, токены `--accent`/`--accent-deep`/`--on-accent` инвертируются штатно, как и везде в приложении.
- 320 px (самый узкий реальный экран) и десктоп — без горизонтального переполнения на обоих экранах.
- `npm run build` — чисто, размер бандла не менялся принципиально (только CSS/иконки/один компонент).

### Осталось

- **S11/S12 по-прежнему актуальны и не сделаны**: установка PWA на реальных Android/iPhone не проверена, `firebase deploy --only hosting` не выполнен (нужен `firebase login` пользователя).
- Исходники макетов в корне проекта (`profile-podvezem.html`, два zip) — не в git, можно удалить, когда не нужны.
- `APP_URL` в GitHub Variables — по-прежнему не задан (нужен домен хостинга).

---

## S11 — PWA, иконка приложения, мобильная полировка

Статус в трекере: 🟡 — код готов и проверен в браузере, **но установка на реальных Android и iPhone не проверена** (см. «Осталось» в конце раздела).

### Логотип: как принималось решение

Своего знака у проекта не было вообще — только палитра «Хвоя». Первый вариант агента (плоская машинка из прямоугольников) пользователя не устроил категорически. Дальше сработала такая схема, её же стоит повторять при похожих задачах: агент показал **четыре разных направления сразу** (маршрут / машина линией / дорога в лесу / монограмма «П»), затем варианты внутри выбранного направления, **каждый раз с превью в 88 px рядом** — иконку на телефоне видно именно такой, и это отсекает варианты, которые красивы только крупно.

Пользователь сгенерировала свои варианты в Midjourney (лежат в `design/references/`) — из них взята **композиция**, но не сам файл: у результатов генератора поля вместо full-bleed, призрачные текстовые артефакты по фону, гуляющая толщина линии. Итог перерисован вектором с нуля.

**Промпты для генераторов иконок — важная деталь:** ни Midjourney, ни Ideogram не рисуют кириллицу; на «букву П» они выдают латинскую P или N. Обход — описывать форму как **греческую Π (Pi)**, её модели знают.

### Иконки

- Мастер-файлы: `design/app-icon.svg` (скругление 116, purpose `any`, из него же apple-touch-icon и favicon) и `design/app-icon-maskable.svg` (полнокадровый, без скругления — форму задаёт система).
- **Знак вписан в габарит 274×274 по центру: диагональ 388 px укладывается в safe zone Android (круг 410 px).** Поэтому maskable-версия не требует отдельного уменьшения знака. **При правке рисунка это условие проверять заново**, иначе маска обрежет дорогу.
- Нарезка: `public/icons/` (192/512 × any/maskable), `public/apple-touch-icon.png` (180), `public/favicon.svg`, `favicon-32.png`, `favicon-16.png`, `public/icons/badge-96.png` (монохромный силуэт для статус-бара Android в push).
- **Как нарезались (пригодится в следующий раз):** ImageMagick/Inkscape/rsvg на машине нет (`convert` в PATH — это системный `convert.exe` Windows для конвертации файловых систем, не ImageMagick, не перепутать). PNG рендерились через canvas в браузере (`mcp__Claude_Browser__javascript_tool`), результат возвращается base64 и превышает лимит вывода — уходит в файл `tool-results/*.txt`, откуда разбирается питоном: содержимое там **дважды** JSON-закодировано и с хвостом `(captured at origin ...)`, который надо отрезать до парсинга.
- **Сжатие:** установлен `Pillow` (`python -m pip install Pillow`), квантизация до 256 цветов с dithering. 512-я иконка: 218 КБ → 12 КБ, бандинга на градиенте нет. В Git Bash вызывать `python`, **не** `python3` — последний резолвится в заглушку WindowsApps и падает с непонятным кодом 49.

### Манифест и мета-теги

- `public/manifest.webmanifest` — `display: standalone`, portrait, `theme_color` `#1F5140`, `background_color` `#ECF0EB`, 4 иконки, 3 shortcut'а.
- `index.html` — манифест, favicon'ы, apple-touch-icon и мета-теги iOS (**iOS манифест не читает: иконку и standalone задают только эти теги**).
- `apple-mobile-web-app-status-bar-style` = **`default`, не `black-translucent`** — шапка приложения светлая (`--surface`), белый текст статус-бара на ней не читался бы. Если шапку когда-нибудь перекрасят в тёмное — этот выбор пересмотреть.

### Подсказка «Добавить на экран»

`src/components/InstallHint.jsx`, подключена в `App.jsx` (не на экранах логина/регистрации). Два принципиально разных пути:
- **Chrome/Android** — перехват `beforeinstallprompt` (с `preventDefault()`, иначе Chrome покажет свою мини-плашку) и своя кнопка «Установить». Событие одноразовое: после `prompt()` его надо обнулить.
- **Safari/iOS** — программного API установки нет вообще, только текстовая инструкция «Поделиться → На экран „Домой“». Chrome/Firefox на iOS (`CriOS`/`FxiOS`) отсеиваются — добавлять на экран они не умеют, подсказка бы только путала.
- Отказ пишется в `localStorage` (`podvezem:install-hint-dismissed`), установленное приложение (`display-mode: standalone` / `navigator.standalone`) подсказку не показывает.

### Главная грабля сессии: без `fetch` в воркере установка не предлагается

**Chrome выдаёт `beforeinstallprompt` только если у активного сервис-воркера есть обработчик `fetch`.** В S10 воркер был, но (а) без `fetch` и (б) регистрировался только при включении уведомлений — то есть кнопка «Установить» на Android не появилась бы вообще, сколько бы ни был правилен манифест.

Что сделано:
- `fetch` добавлен **в тот же** `public/firebase-messaging-sw.js`. Отдельный второй воркер завести нельзя: **два воркера на scope `/` не уживаются, второй вытесняет первый** и ломает push.
- Стратегия: навигация — network-first с откатом на кэш `/` (свежая сборка подхватывается сразу, но в метро приложение откроется); `/assets/*` (хэшированные Vite, неизменные) — cache-first; **чужие origin не трогаются вообще** (Firestore, FCM, Google Fonts — у них своя логика).
- Регистрация воркера перенесена из `src/lib/notifications.js` в `src/main.jsx`, происходит при старте. **Только в PROD** — в dev кэш воркера конфликтует с HMR Vite.
- Отсюда: **установку и офлайн проверять только на боевой сборке.** В `.claude/launch.json` добавлена конфигурация `podvezem-preview` (порт 4173).

### Прочее

- Знак в шапке (`AppHead`) переведён с иконки `bus` на новую `brand` — тот же силуэт, что на домашнем экране. В `Icon.jsx` добавлены `brand` и `close`.
- Закрыт отложенный пункт S10: **иконка и badge в push** — `notify/index.mjs` и зеркально `functions/lib/messaging.js`. Оба поля требуют абсолютный URL, поэтому лежат в одной ветке с `link`: **без `APP_URL` push уходит без ссылки и без иконки** (деградация, не ошибка отправки).

### Проверено в этой сессии

- Все ресурсы PWA отдаются с верными MIME (`application/manifest+json`, `image/png`, `image/svg+xml`), ошибок в консоли нет.
- Горизонтального переполнения нет на 375 и **320 px** (самый узкий реальный экран) — доска, профиль, форма новой поездки, «мои поездки», в светлой и тёмной темах.
- На боевой сборке воркер регистрируется и активируется на scope `/`, кэш `podvezem-v1` наполняется оболочкой и ассетами.
- **Офлайн подтверждён по-настоящему:** preview-сервер остановлен, страница перезагружена — приложение отрисовалось из кэша со стилями и шрифтами.
- `npm run build` — чисто, 871 КБ (было 869 КБ на конец S10).

### Осталось

- **Установка на реальных устройствах.** В автоматизированном браузере `beforeinstallprompt` не выдаётся (Chrome ждёт настоящего пользовательского engagement), iOS-ветку может проверить только реальный Safari. Проверять: Android — появляется ли кнопка «Установить» и ставится ли приложение; iPhone — «Поделиться» → «На экран „Домой“», открытие без адресной строки. Только после этого S11 → ✅.
- `APP_URL` в GitHub Variables (S12) — сейчас push без ссылки и без иконки.
- Разнесение бандла по чанкам — по-прежнему S12.

---

## S10 — push-уведомления (предыдущая сессия)

Дата: 03.09.2026. Сессия: **S10 — push-уведомления**, продолжение той же даты — **пивот с Cloud Functions на GitHub Actions polling**, см. новый раздел «Пивот: Blaze заблокирован → polling через GitHub Actions» ниже (он главный, читать в первую очередь для этой фичи). Исходный текст хэндоффа про Cloud Functions/Blaze оставлен ниже как есть — сам код `functions/` не удалён, но сейчас не используется.

## Пивот: Blaze заблокирован → polling через GitHub Actions

После публикации хэндоффа ниже пользователь попытался включить Blaze и упёрся в блокировку на стороне Google: карта прошла верификацию (деньги за холд вернулись автоматически через пару минут — с оплатой всё в порядке), но и первый billing account, и специально созданный второй закрылись почти сразу с вердиктом **"You can't reopen this billing account because this account is not in good standing"**. Кнопка Reopen неактивна у обоих — это финальное решение антифрод-системы Google, не техническая ошибка и не что-то, что чинится повторным созданием третьего аккаунта (два из двух закрылись одинаково быстро — проблема не в конкретном аккаунте/карте). Пользователь решил не ждать ответ поддержки Google, а реализовать push без Cloud Functions.

**Решение:** тот же набор из 6 сценариев ТЗ 10, но вместо мгновенного триггера Firestore (`onDocumentCreated`/`onDocumentUpdated`, требует Blaze) — периодический опрос из GitHub Actions раз в 10 минут. Admin SDK (сервисный аккаунт) для чтения/записи Firestore и отправки FCM **не требует Blaze вообще** — это ограничение только у Cloud Functions как продукта, а не у Firestore/Messaging API как таковых.

**Что сделано:**
- `notify/index.mjs` (новый пакет, ESM) — логика всех 6 сценариев перенесена из `functions/lib/*Triggers.js` практически без изменений (тексты уведомлений, получатели, условие пропуска шаблонных предложений S9 — всё то же), только вместо event-триггера — функции `pollNewRideOffers`/`pollNewRequests`/`pollNewResponses`/`pollResponseStatusChanges`/`pollNewRequestResponses`/`pollRequestResponseStatusChanges`, каждая опрашивает свою коллекцию через `where(<поле>, '>', курсор).orderBy(<поле>)`.
- Курсоры (последний обработанный момент по каждому из 6 сценариев) хранятся в `system/notifyState` — коллекция видна только Admin SDK, в `firestore.rules` её нет и не должно быть. **При первом запуске** курсоров ещё нет — скрипт выставляет их на «сейчас» и завершается без обработки, иначе первый же прогон разослал бы push по всей уже существующей базе (все текущие открытые предложения на доске и т.п.). Это ожидаемое поведение первого запуска, не баг.
- `src/lib/db.js` — в шесть функций смены статуса отклика (`confirmResponse`, `rejectResponse`, `cancelResponse`, `confirmRequestResponse`, `rejectRequestResponse`, `cancelRequestResponse`) добавлено поле `updatedAt: serverTimestamp()`. Раньше это поле не требовалось — Cloud Functions получали событие изменения от самого Firestore и не нуждались в отдельном таймстампе; polling же должен как-то отличать «статус сменился только что» от «отклик существует уже неделю», отсюда и добавка. Firestore rules трогать не пришлось — правила `responses`/`requestResponses` разрешают update целиком обоим участникам, без ограничения по конкретным полям (`hasOnly`), в отличие от `rideOffers`.
- `.github/workflows/notify.yml` — `cron: '*/10 * * * *'` + `workflow_dispatch` для ручного запуска из вкладки Actions, `concurrency: {group: notify-polling, cancel-in-progress: false}` чтобы соседние прогоны не пересекались, если предыдущий почему-то подзавис. Секрет `FIREBASE_SERVICE_ACCOUNT` (полный JSON) пишется во временный файл, `GOOGLE_APPLICATION_CREDENTIALS` указывает на него — стандартный паттерн для Admin SDK в CI, ничего специфичного для этого проекта.
- `functions/` (пакет Cloud Functions из основной части хэндоффа ниже) **не удалён и не менялся** — если Google когда-нибудь разблокирует Blaze (после ответа поддержки, которого пользователь пока не дожидается), его можно задеплоить как есть вместо `notify/`, никакого расхождения в логике уведомлений между ними нет — `notify/index.mjs` был построен как прямой перенос `functions/lib/*.js`.

**Проверено в этой сессии:**
- `npm run build` в корне проекта — чисто, размер бандла не изменился (только `src/lib/db.js` тронут, шесть строк).
- `npm install` в `notify/` — прошёл (то же ожидаемое предупреждение `EBADENGINE` про Node 20 vs 24, что и у `functions/`, не мешает).
- `node --check notify/index.mjs` — синтаксис корректен.
- Пробный запуск `node notify/index.mjs` без реальных credentials — упал ровно на `Unable to detect a Project Id` внутри первого обращения к Firestore (`loadCursors` → `system/notifyState.get()`), то есть весь код до этой точки (импорты, инициализация, структура) отработал без ошибок — дальше нужны только настоящие credentials, которых у агента нет.

**Сделано и проверено пользователем в этой же сессии (03.09.2026, после пуша в main):**
1. Сервисный аккаунт создан (Firebase Console → Project settings → Service accounts → Generate new private key) и добавлен как секрет `FIREBASE_SERVICE_ACCOUNT` в GitHub → Settings → Secrets and variables → Actions.
2. Первый ручной прогон workflow (`Run workflow`) — **Success**, лог показал ожидаемое сообщение про инициализацию курсоров на первом запуске (не ошибка, см. код `loadCursors` выше).
3. **Сквозная проверка сценария "новое предложение → push заказчику" на реальном iPhone — пройдена полностью:** вход через ngrok-туннель (`ngrok http 5173`, Vite потребовал добавить `allowedHosts: true` в `vite.config.js` — без этого Vite сам блокирует запросы с чужим Host-заголовком, стандартная защита от DNS rebinding, никак не связанная с Firebase), добавление на «Домой» через Safari, включение тумблера в профиле → с компьютера (`...-02`) опубликовано новое предложение → ручной запуск workflow → **push пришёл на iPhone**.
4. `vite.config.js` — добавлен `server.allowedHosts: true` (dev-only, на прод-сборку не влияет) — нужен для тестирования через любой HTTPS-туннель (ngrok/localtunnel), домен которого каждый раз новый на бесплatном тарифе.

**Осталось (не блокирует статус "готово", можно сделать по мере необходимости):**
- Проверить оставшиеся 3 сценария из 4 (новый отклик, подтверждение/отказ, отмена) — по той же схеме, что и выше.
- Проверить сценарий "новый запрос → push водителям" (симметричный уже проверенному).
- (Опционально) переменную `APP_URL` в GitHub → Settings → Variables — до появления домена хостинга (S12) можно не задавать.
- Удалить скачанный JSON сервисного аккаунта с диска, если он ещё лежит где-то локально — чувствительный файл, `serviceAccountKey.json` уже в `.gitignore` на случай, если попадёт в корень проекта.

## Важно: эта сессия начата в новой папке

В отличие от S1–S9, эта сессия работала в **свежем клоне** репозитория (`git clone` в новую папку), а не в папке с предыдущими сессиями. `.env.local` в git не попадает — пользователь заново прислал ключи `firebaseConfig` и VAPID-ключ в чат, файл `.env.local` создан вручную с ними. Если следующая сессия тоже стартует в новой папке — этот шаг снова понадобится (ключи проекта `podvezyom` см. в Firebase Console, VAPID — в Cloud Messaging → Web Push certificates).

## Что сделано в S10

Решения в начале сессии (обсуждались с пользователем):
- **Cloud Functions на тарифе Blaze**, не клиентские триггеры. Пользователю показан примерный расчёт стоимости — при масштабе одного посёлка реалистичный счёт около 0 ₽/мес (FCM бесплатен всегда; Cloud Functions и Firestore на Blaze сохраняют те же бесплатные квоты, что и на Spark, платится только превышение). Пользователь согласился на Blaze.
- **Адресаты broadcast-уведомлений о новом предложении/запросе — все пользователи противоположной роли**, без фильтра по очереди (закрывает открытый вопрос из ТЗ 11 и из трекера). Решение пользователя.

### Серверная часть — `functions/` (новый пакет, Cloud Functions 2-го поколения)

- `functions/lib/messaging.js` — `sendToUser(uid, tokens, payload)` (`admin.messaging().sendEachForMulticast`, чистит невалидные токены из `users/{uid}.notificationTokens` через `arrayRemove` по кодам ошибок `registration-token-not-registered`/`invalid-argument`/`invalid-registration-token`), `sendToUid` (читает токены пользователя сама), `sendToUsers` (рассылка списку пользователей).
- `functions/lib/broadcastTriggers.js` — `onRideOfferCreated` (новое предложение → всем `isCustomer`; **пропускает** документы с полем `templateId`, иначе публикация повторяющегося шаблона S9 на месяц вперёд шлёт каждому заказчику до 30 push одним действием водителя — решение принято по ходу, не обсуждалось отдельно с пользователем, но напрямую следует из механики S9), `onRequestCreated` (новый запрос → всем `isDriver`).
- `functions/lib/responseTriggers.js` — `onResponseCreated` (новый отклик заказчика → водителю-владельцу предложения), `onResponseUpdated` (смена `status`: `confirmed`/`rejected` → заказчику-автору отклика, `cancelled` → водителю — по коду `db.js`, отмену подтверждённого отклика всегда делает заказчик, п. 7.7 ТЗ).
- `functions/lib/requestResponseTriggers.js` — зеркально предыдущему для откликов водителей на запросы (`onRequestResponseCreated`/`onRequestResponseUpdated`, роли развёрнуты: `cancelled` инициирует заказчик п. 8.4 ТЗ → уведомляется водитель).
- **`webpush.fcmOptions.link` требует абсолютный HTTPS-URL** — у проекта его пока нет, хостинг только в S12. Решение: ссылка в push добавляется, только если в `functions/.env` задан `APP_URL` (шаблон — `functions/.env.example`); без него push уходит без ссылки-перехода (клик просто открывает/фокусирует вкладку) — деградация, не ошибка отправки. **Не забыть задать `APP_URL` в S12**, когда появится домен хостинга.
- Правила Firestore **не менялись** — токены хранятся полем `notificationTokens` в `users/{uid}`, который и так пишет только владелец (правило `S1` не изменилось); Cloud Functions читают всех пользователей через Admin SDK в обход правил, им это не нужно.
- `functions/package.json` (`firebase-admin`, `firebase-functions`, `engines.node: "20"`), `functions/.gitignore`, `.firebaserc` (`{"projects":{"default":"podvezyom"}}`), `firebase.json` (только `functions.source`). Зависимости установлены (`npm install` в `functions/`), `require('./functions/index.js')` проверен вручную — грузится и экспортирует все шесть функций без ошибок. **Firebase CLI (`firebase-tools`) установлен глобально** на этой машине.

### Клиентская часть

- `src/lib/notifications.js` — `enableNotifications(uid)` (запрашивает разрешение → регистрирует `/firebase-messaging-sw.js` → `getToken` → сохраняет в профиль), `disableNotifications(uid)`, `getCurrentToken()` (не спрашивает разрешение, только читает уже выданный токен — на нём строится состояние тумблера, не на `Notification.permission` напрямую: разрешение браузера липкое и код не может его сбросить программно при отключении, а вот регистрация токена в профиле — да), `listenForegroundMessages()`. Обёртка над `firebase/messaging`, `isSupported()` из самого SDK отсекает недоступные контексты (в т. ч. iPhone-вкладку вне PWA — там просто нет `Notification`).
- `src/lib/db.js` — `addNotificationToken`/`removeNotificationToken` (`arrayUnion`/`arrayRemove` на `users/{uid}.notificationTokens`, массив — не одно поле, чтобы push работал на нескольких устройствах пользователя разом).
- `src/components/NotificationsToggle.jsx` (новый) — чекбокс в профиле: статус поддержки, статус разрешения, ошибки понятным текстом (`permission-denied` → «заблокированы в настройках браузера», без деталей о токене). Подключён в `src/screens/Profile.jsx`.
- `src/App.jsx` — слушает foreground-сообщения (вкладка открыта и в фокусе — SDK не показывает их сама, только фоновые показывает сервис-воркер), показывает через `new Notification()`.
- `public/firebase-messaging-sw.js` (новый) — конфиг Firebase захардкожен внутри файла: `public/` Vite не обрабатывает, `import.meta.env` там не подставляется, а ключ веб-приложения Firebase не секрет (защита — правила Firestore/Auth, не он).
- `.env.example` уже содержал `VITE_FIREBASE_VAPID_KEY` с прошлой сессии — просто заполнен.

## Проверено / не проверено

**Проверено в браузере** (тестовый аккаунт `+7 900 222-33-01`, свежий `npm install` + `npm run build` — чисто, бандл 869 КБ было 836 КБ):
- Приложение стартует, логин проходит, доска водителей рендерится без ошибок в консоли.
- Экран профиля: тумблер уведомлений рендерится, корректно определяет неподдерживаемое/заблокированное состояние (`Notification.permission === 'denied'` в этой браузерной песочнице — см. ниже) и показывает соответствующий текст.
- `/firebase-messaging-sw.js` отдаётся по прямому запросу (200, `text/javascript`).
- `require('./functions/index.js')` в Node — грузится без ошибок, экспортирует все 6 функций.

**Не проверено — окружение этой сессии не позволяет:**
- Реальный запрос разрешения на уведомления и получение push. Автоматизированный браузер в этой сессии сам блокирует уведомления (политика песочницы) — `Notification.permission` там всегда `'denied'`, окно с настоящим запросом `Notification.requestPermission()` никогда не появляется. Код для этого пути написан и статически корректен (см. `enableNotifications`), но ветка «пользователь нажал „Разрешить“ → токен сохранился в `users/{uid}.notificationTokens` → пришёл push» нигде не выполнялась целиком.
- Все четыре сценария push с реальной доставкой (новое предложение/запрос, новый отклик, подтверждение/отказ, отмена) — функции не задеплоены (см. ниже), доставлять push неоткуда.
- iOS-сценарий (добавление на «Домой», ограничение ТЗ 10) — нужен реальный iPhone.

## Что осталось — обязательно перед тем, как S10 можно считать «готово» в трекере

Три шага, которые нужно сделать пользователю (агент до них не достаёт):

1. **Включить тариф Blaze** в Firebase Console (project `podvezyom` → Settings → Usage and billing → Modify plan) — привязка карты. Решение принято, пользователь согласен на расход ~0 ₽/мес при таком масштабе.
2. **`firebase login`** из терминала (это Google-аккаунт пользователя, агент выполнить его не может), затем из корня проекта:
   ```
   firebase deploy --only functions
   ```
   Firebase CLI (`firebase-tools`) уже установлен глобально на машине.
3. **Проверить на реальных устройствах** все четыре сценария из ТЗ 10, включая iPhone (добавить сайт на «Домой» через Safari, iOS 16.4+ — из обычной вкладки push не работают).

После этого — обновить статус S10 в трекере на ✅ и в этом хэндоффе (или добавить хэндофф следующей сессии) зафиксировать результат реальной проверки.

## Особенности окружения (не меняются с S1, плюс новое из S10)

- **Node не в PATH.** `C:\Program Files\nodejs\node.exe` (v24.18.0 на момент S10, npm 11.16.0). В PowerShell: `$env:Path = "C:\Program Files\nodejs;$env:Path"`.
- **`functions/package.json` просит Node 20** (Cloud Functions 2-го поколения не поддерживают 24) — при `npm install` в `functions/` будет предупреждение `EBADENGINE`, это ожидаемо и не мешает: локальный Node нужен только чтобы `npm install`/статически проверить require, реальный рантайм в облаке — свой, версию берёт из `engines.node`.
- **GitHub CLI не в PATH** — `& "C:\Program Files\GitHub CLI\gh.exe"`.
- **Vite слушает только `::1`** без явного host. В `vite.config.js` прописан `host: '127.0.0.1'`. Не убирать.
- Git-идентичность в этой папке взята из глобального конфига машины (`djachkova-ux` / `djachkova@kefir-media.com`) — совпадает с реальным аккаунтом пользователя, локального оверрайда не потребовалось (в отличие от заметки про `alati74` в более старой копии проекта на этой же машине — та относится к другой локальной папке, не к этой).
- **Firebase-проект:** `podvezyom`. **Тариф — Spark на момент конца S10** (переход на Blaze размечен как шаг за пользователем, см. выше). Firestore rules по-прежнему публикуются только вручную через Console → Rules → Publish, CLI-деплой rules не настроен (в S10 это не понадобилось — правила не менялись). **Firebase CLI (`firebase-tools`) установлен глобально в S10** — раньше не был нужен вообще, теперь нужен для `firebase deploy --only functions`.
- **Порт dev-сервера** — в этой сессии запускался напрямую через `node .../vite.js podvezem` (не через `npm run dev`, так как сессия работала из родительской папки `D:\Работа\Школьный трансфер`, а не из `podvezem`); добавлен `D:\Работа\Школьный трансфер\.claude\launch.json`, отдельный от `podvezem\.claude\launch.json` (тот остаётся для запуска изнутри самой папки `podvezem`, если следующая сессия будет работать оттуда).

## Принятые решения (накопительно)

- Push — через Cloud Functions на Blaze, не клиентские триггеры (см. «Что сделано» выше).
- Broadcast-уведомления (новое предложение/запрос) — всем противоположной роли, без фильтра по очереди. Закрывает открытый вопрос ТЗ 11.
- Предложения из шаблона (`templateId` есть) не триггерят broadcast-push — иначе шаблон на месяц вперёд спамит.
- Ссылка в push (`fcmOptions.link`) — опциональна через `APP_URL` в `functions/.env`, до появления домена хостинга (S12) просто не добавляется.
- Иконка в push не задана — иконок PWA ещё нет (S11), браузер покажет свою по умолчанию; можно добавить `webpush.notification.icon` в `functions/lib/messaging.js`, когда иконки появятся.

## На что обратить внимание дальше

- **S11 (PWA)**: заодно задать иконку в push-уведомлениях (см. выше), когда появятся иконки PWA.
- **S12 (деплой)**: задать `APP_URL` в `functions/.env` и передеплоить функции (`firebase deploy --only functions`), чтобы push получили кликабельную ссылку на нужный экран.
- **Тестовые данные**: аккаунты `+7 900 222-33-0N` (`N=1..4`) / `test1234`, `01` также заказчик. У Дмитрия (`...-02`) повторяющийся шаблон с открытыми предложениями (см. хэндофф S9) — почистить все тестовые данные перед показом первым пользователям.
- **Размер бандла**: 869 КБ (было 836 КБ на конец S9) — прирост от `firebase/messaging`. Разнесение по чанкам — S11/S12, как и планировалось.
- **После первого реального теста push** — свериться со счётом в Google Cloud Billing и поставить budget alert (агент сделать не может, консоль недоступна).
